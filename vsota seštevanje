#include <stdio.h>

int main(void) {
  int x = 1, vsota = 0;
  while (1){
    scanf("%d", &x);
    if (x==0) break;
    vsota = vsota + x;
    printf("vsota: %d\n", vsota);
  }
}


#include <stdio.h>

int sestej(int a, int b) {
  return a + b;
}

int main(void) {
  printf("%d", sestej(5,6));
}
